#!/bin/bash
 
source <%= @_virtualenv %>/bin/activate

export DJANGO_SETTINGS_MODULE=<%= @settings_module %>

cd <%= @_srcdir %>

# Source and export environment variables for app
# if any.
#
if [ -r .env ]; then
  set -a
  source .env
  set +a
fi

exec <%= @command %>
